services:
  - type: web
    name: aether-agent-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: AETHER_MODEL_URL
        sync: false  # Set manually in Render Dashboard after model upload
