# AetherAgent – Utvecklingsplan 2026-03-21
**Status:** Aktiv
**Datum:** 2026-03-21

---

## Sammanfattning

AetherAgent har nått Fas 16 – alla kärnfaser är klara. Projektet har gått från ren HTML-parser till en fullständig LLM-native browsermotor med 69 WASM-funktioner, 48 HTTP-endpoints, 32 MCP-tools.

**Fas 17: AetherDOM** är nästa stora milstolpe — en fullständig DOM-implementation med Arena-baserad allokering, Boa DOM Bridge, och hydration extraction.

---

## Nuläge — Kritiska begränsningar

```
HTML → html5ever → RcDom (Rc<Node>) → SemanticBuilder → SemanticTree → JSON → LLM
                        ↑                     ↑
                   heap-allokerat         Boa kan bara eval:a
                   ~1000 alloc/sida       rena uttryck (1+2)
                   cache-hostile          ingen DOM-access
```

| Problem | Nuvarande lösning | Konsekvens |
|---------|-------------------|------------|
| JS kan inte nå DOM | `js_eval.rs` blockar `document.*` | React/Vue/Angular-sidor = tomma skelett |
| Ingen querySelector | Regex-mönster i `js_bridge.rs` | Missar 90% av verkliga DOM-mönster |
| RcDom = långsamt | `markup5ever_rcdom` (Rc) | ~1000 heap-alloc/sida, cache-hostile |
| Ingen hydration | Ramverkdata ignoreras | SSR-sidor: kör JS i onödan |
| Ingen layout | Stubbar i `vision_backend.rs` | `getBoundingClientRect()` = nonsens |

### Vad som fungerar bra (ska INTE röras)

- SemanticBuilder pipeline (`parser.rs` → `semantic.rs` → `trust.rs`)
- Aho-Corasick injektionsdetektion
- Goal-driven streaming (95–99% token savings)
- Firewall + trust-system
- Vision pipeline (YOLOv8)
- MCP/HTTP/WASM API-yta

---

## Fas 17: AetherDOM — Fullständig arkitektur

### Arkitektur: Hur AetherDOM integreras

```
┌─────────────────────────────────────┐
│          NYTT: Tier 0               │
│    Hydration State Extraction       │
│  NEXT_DATA, __NUXT__, ng-state...  │
│  → Extrahera data UTAN att köra JS  │
└──────────────┬──────────────────────┘
               │ (om data hittades → KLAR)
               │ (om ej → fortsätt ↓)
┌──────────────┴──────────────────────────────────┐
│            NYTT: Arena DOM (SlotMap)              │
│                                                  │
│   SlotMap<NodeKey, DomNode>  ← ersätter RcDom    │
│   ├── 1024 pre-allokerade noder                  │
│   ├── generational indices (stale-ref safety)    │
│   ├── cache-friendly Vec-layout (5-10x DFS)      │
│   └── Boa-kompatibel: #[derive(Trace, Finalize)] │
│                                                  │
│   servo/selectors integration:                   │
│   ├── querySelector() / querySelectorAll()       │
│   ├── Selectors Level 4 (:has, :is, :nth-child)  │
│   └── Bloom filter för ancestor-matching         │
└──────────────┬──────────────────────────────────┘
               │
┌──────────────┴──────────────────────────────────┐
│          NYTT: Boa DOM Bridge (boa_class)         │
│                                                  │
│   #[boa_class] structs exponerade som JS-objekt: │
│                                                  │
│   JsDocument: getElementById, querySelector,     │
│     querySelectorAll, createElement, body, head   │
│   JsElement: textContent, innerHTML, classList,  │
│     style, getAttribute, setAttribute, dataset    │
│   JsNode: parentNode, childNodes, appendChild,   │
│     removeChild, insertBefore, cloneNode          │
│   JsWindow: location, navigator stubs            │
│   + JsNodeList, JsDOMTokenList, JsHTMLCollection │
│   + JsCSSStyleDeclaration (stubbad)              │
│   + JsEvent (preventDefault, stopPropagation)    │
│                                                  │
│   context.register_global_property("document")   │
│   context.register_global_property("window")     │
│   + boa_runtime: console, URL, TextEncoder       │
└──────────────┬──────────────────────────────────┘
               │
┌──────────────┴──────────────────────────────────┐
│       BEFINTLIG PIPELINE (oförändrad)            │
│                                                  │
│   Arena DOM → SemanticBuilder → trust.rs →       │
│     SemanticTree → JSON                          │
│                                                  │
│   Alla befintliga moduler fungerar som innan:    │
│   streaming, grounding, vision, firewall,        │
│   causal, compiler...                            │
└──────────────────────────────────────────────────┘
```

---

### Progressive Escalation Pipeline (Tier 0–4)

Nyckeln — köra minsta möjliga arbete per sida:

**Tier 0: Hydration Extraction (0 ms JS)**
- Regex/JSON-parser för `__NEXT_DATA__`, `__NUXT__`, `ng-state`, etc.
- 10+ ramverk: Next.js, Nuxt, Angular, Remix, Apollo, Gatsby, SvelteKit, Qwik, Astro, Next.js App Router (Flight Protocol)
- Om data extraheras → bygg SemanticTree direkt, SKIPPA JS
- **Uppskattning: täcker ~40% av moderna webbsidor**

**Tier 1: Statisk HTML Parse (nuvarande, ~1 ms)**
- `html5ever` → Arena DOM → SemanticBuilder
- Ingen JS-exekvering
- Fungerar för: statiska sidor, server-rendered utan hydration

**Tier 2: Sandboxed JS med DOM (NYTT, ~10–50 ms)**
- Boa + Arena DOM + `boa_class` bridges
- Kör `<script>`-block mot verklig DOM-representation
- Firewall-filtrering av fetch()/XHR (redan implementerat)
- MutationObserver-shim → fånga DOM-ändringar
- rAF-shim → köra animationsramverk
- Fungerar för: React SSR hydration, Vue client-side, Svelte

**Tier 3: Blitz Render (nuvarande, ~10–50 ms)**
- Ren Rust HTML/CSS rendering
- Layout-beräkning (riktig `getBoundingClientRect`!)
- Fungerar för: CSS-beroende layout, visuell grounding

**Tier 4: Chrome CDP (nuvarande, ~500–2000 ms)**
- Fullständig browser
- Allt stöds
- Fallback för: komplexa SPAs, WebGL, Canvas, Web Workers

**Intelligent escalation via TierHint:**
```rust
fn select_tier(html: &str, url: &str) -> Tier {
    // Tier 0: Hydration data finns?
    if let Some(data) = extract_hydration_state(html) {
        return Tier::Hydration(data);
    }
    // Tier 1: Ingen JS alls?
    let js = detect_js_snippets(html);
    if js.total_inline_scripts == 0 && !js.has_framework {
        return Tier::StaticParse;
    }
    // Tier 2: JS men inga tunga features?
    if !needs_layout(html) && !needs_canvas(html) {
        return Tier::BoaDOM;
    }
    // Tier 3: Behöver layout men inte full browser?
    if !needs_full_browser(html) {
        return Tier::BlitzRender;
    }
    // Tier 4: Allt annat
    Tier::ChromeCDP
}
```

---

### DOM API — Prioritering

**Kritisk (Vecka 1–2) — 25 metoder, täcker React+Vue:**

| Interface | Metoder |
|-----------|---------|
| Document | `getElementById`, `querySelector`, `querySelectorAll`, `createElement`, `createTextNode`, `body`, `head`, `documentElement` |
| Element | `getAttribute`, `setAttribute`, `removeAttribute`, `classList` (add/remove/toggle/contains), `style` (getPropertyValue/setProperty), `innerHTML`, `textContent`, `id`, `className`, `tagName` |
| Node | `appendChild`, `removeChild`, `insertBefore`, `parentNode`, `childNodes`, `firstChild`, `nextSibling`, `nodeType`, `cloneNode` |

**Viktig (Vecka 3–4) — 30 metoder, täcker Angular+Svelte:**

| Interface | Metoder |
|-----------|---------|
| Element | `closest`, `matches`, `getBoundingClientRect` (stubbad), `addEventListener`, `removeEventListener`, `dispatchEvent`, `children`, `firstElementChild`, `nextElementSibling`, `outerHTML`, `dataset` |
| Document | `createDocumentFragment`, `getElementsByClassName`, `getElementsByTagName`, `createEvent` |
| NodeList | `length`, `item`, `forEach`, `entries` |
| HTMLCollection | `length`, `item`, `namedItem` |
| DOMTokenList | `length`, `value`, `add`, `remove`, `toggle`, `contains`, `replace` |

**Stubbad (aldrig riktig implementation):**
- `getComputedStyle()` → `{"display": "block", "visibility": "visible", "opacity": "1"}`
- `getBoundingClientRect()` → `{x:0, y:0, width:1024, height:768}` (eller Blitz-data om Tier 3)
- `window.innerWidth/Height` → 1024/768
- `localStorage/sessionStorage` → in-memory Map (redan blockerat av firewall)

---

### Hydration Extraction — 10+ ramverk utan JS

```rust
fn extract_hydration_state(html: &str) -> Option<HydrationData> {
    // Next.js Pages Router
    if let Some(data) = extract_next_data(html) {        // <script id="__NEXT_DATA__">
        return Some(HydrationData::NextJs(data));
    }
    // Next.js App Router (React Flight Protocol)
    if let Some(data) = extract_next_flight(html) {      // self.__next_f.push([...])
        return Some(HydrationData::NextFlight(data));
    }
    // Nuxt.js
    if let Some(data) = extract_nuxt_data(html) {        // window.__NUXT__=
        return Some(HydrationData::Nuxt(data));
    }
    // Angular Universal
    if let Some(data) = extract_angular_state(html) {    // <script id="ng-state">
        return Some(HydrationData::Angular(data));
    }
    // Remix
    if let Some(data) = extract_remix_context(html) {    // window.__remixContext
        return Some(HydrationData::Remix(data));
    }
    // ... Apollo, Gatsby, SvelteKit, Qwik, Astro
    None
}
```

**Varför detta är enormt:** ~40% av moderna produktionssidor använder SSR med hydration. Om vi kan extrahera datan direkt → 0 ms JS-exekvering, komplett data.

---

### Arena DOM — SlotMap ersätter RcDom

```rust
use slotmap::{SlotMap, new_key_type};

new_key_type! { pub struct NodeKey; }

#[derive(Trace, Finalize, JsData)]  // Boa GC-kompatibel
pub struct ArenaDom {
    nodes: SlotMap<NodeKey, DomNode>,
    document_key: NodeKey,
}

pub struct DomNode {
    pub node_type: NodeType,
    pub tag: Option<String>,
    pub attributes: Vec<(String, String)>,
    pub text: Option<String>,
    pub parent: Option<NodeKey>,
    pub children: Vec<NodeKey>,
    pub classes: Vec<String>,
    pub id: Option<String>,
}
```

| Egenskap | RcDom (nu) | SlotMap Arena |
|----------|-----------|---------------|
| Allokering | ~1000 heap-alloc/sida | 1 alloc (pre-allokerad Vec) |
| Cache | Hostile (Rc spridda) | Friendly (kontiguöst minne) |
| DFS-traversering | 1x (baseline) | 5–10x snabbare |
| Stale references | Möjlig (Rc cykler) | Omöjlig (generational index) |
| Boa-integration | Kräver wrapping | Direkt (#[derive(Trace)]) |
| WASM-storlek | Minimal | +50KB (slotmap crate) |

---

### Integrationspunkter i befintlig kod

| Befintlig modul | Ändring | Påverkan |
|-----------------|---------|----------|
| `parser.rs` | Byt RcDom → ArenaDom | Intern refactor, samma output |
| `semantic.rs` | Läs från ArenaDom istället för RcDom | `SemanticBuilder::traverse()` ändras |
| `js_eval.rs` | Registrera DOM-objekt i Boa Context | `eval_js()` → `eval_js_with_dom()` |
| `js_bridge.rs` | Ta bort regex-baserad DOM-matching | `selective_exec()` → riktig JS-eval |
| `stream_engine.rs` | Ingen ändring | Läser SemanticTree som innan |
| `intercept.rs` | Boa-fetch → firewall (redan implementerat) | Minimal ändring |
| `vision_backend.rs` | `getBoundingClientRect` → Blitz-data | Bättre visuell grounding |
| `lib.rs` | Ny WASM-funktion: `parse_with_dom(html, goal, url)` | Additiv, inget bryts |
| `Cargo.toml` | +slotmap, +servo-selectors, feature: `dom` | Nytt feature-gate |

**Kritiskt:** Inget befintligt API bryts. `parse_to_semantic_tree()`, `parse_with_js()`, alla endpoints — allt fungerar som innan. ArenaDom är en intern implementation detail.

---

### Implementationsordning

| Prio | Komponent | Uppskattning | Värde |
|------|-----------|-------------|-------|
| P0 | Hydration extraction (Tier 0) | 3–5 dagar | Enormt — 40% av webben utan JS |
| P1 | Arena DOM (SlotMap) | 3–4 dagar | 5–10x snabbare traversering |
| P2 | Boa DOM Bridge (25 kritiska metoder) | 5–7 dagar | React/Vue fungerar |
| P3 | servo/selectors integration | 2–3 dagar | Full querySelector |
| P4 | 30 extra DOM-metoder | 3–4 dagar | Angular/Svelte fungerar |
| P5 | Progressive escalation logic | 2–3 dagar | Automatisk tier-val |
| P6 | MutationObserver + rAF shim | 2–3 dagar | SPA-ramverk fungerar fullt |

---

### Konkurrentanalys

| Kapabilitet | AetherAgent+DOM | LightPanda | Stagehand | browser-use | spider-rs |
|-------------|-----------------|------------|-----------|-------------|-----------|
| Semantic perception | ✅ Goal-driven | ❌ | ✅ (Chrome A11y) | ❌ | ❌ |
| Prompt injection | ✅ Aho-Corasick | ❌ | ❌ | ❌ | ❌ |
| JS execution | ✅ Boa+DOM | ✅ SpiderMonkey | ✅ Chrome V8 | ✅ Chrome V8 | ❌ |
| DOM API | ✅ ~85 metoder | ✅ Full | ✅ Full | ✅ Full | ❌ |
| WASM-embedded | ✅ | ❌ | ❌ | ❌ | ❌ |
| Hydration extract | ✅ 10+ ramverk | ❌ | ❌ | ❌ | ❌ |
| Token savings | ✅ 95–99% | ❌ | ~50% | ❌ | ❌ |
| Causal reasoning | ✅ find_safest_path | ❌ | ❌ | ❌ | ❌ |
| Streaming parse | ✅ LLM-directives | ❌ | ❌ | ❌ | ❌ |
| Vision (YOLOv8) | ✅ Built-in | ❌ | GPT-4V | GPT-4V | ❌ |
| MCP server | ✅ 32 tools | ❌ | ❌ | ❌ | ❌ |
| Hastighet | ~1–5ms stat, ~100ms SPA | ~300ms | ~2–5s | ~3–10s | ~13ms |
| Säkerhet | ✅ Firewall+Trust | ❌ | ❌ | ❌ | ❌ |

**Ingen konkurrent har kombinationen:** Semantic perception + prompt injection protection + WASM-embeddable + hydration extraction + goal-driven streaming + causal action graph + vision + DOM API + MCP

---

## Teknisk skuld att åtgärda

- [ ] Verifiera att alla 28 källfiler har enhetstester
- [ ] `cargo clippy -- -D warnings` — noll varningar
- [ ] `cargo fmt --check` — noll diffs
- [ ] Granska `Cargo.toml` för oanvända dependencies
- [ ] Kontrollera att WASM-build < 6MB

---

*Plan skapad: 2026-03-21 · Branch: `claude/investigate-boa-js-KLiO4`*
